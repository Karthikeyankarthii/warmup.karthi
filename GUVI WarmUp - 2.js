/* Fill in your code that takes an number minutes and converts it to seconds. */

function toSeconds(minutes) {
	return minutes * 60;
}
console.log(toSeconds(5)); // Outputs: 60
console.log(toSeconds(3)); // Outputs: 300
console.log(toSeconds(2)); // Outputs: 600